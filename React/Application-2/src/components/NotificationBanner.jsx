import React from 'react';

const NotificationBanner = ({ message }) => (
  <div style={{ backgroundColor: '#f0f8ff', padding: '10px', margin: '10px 0',color:'black' }}>{message}</div>
);

export default NotificationBanner;